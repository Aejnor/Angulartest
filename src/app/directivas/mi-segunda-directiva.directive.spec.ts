import { MiSegundaDirectivaDirective } from './mi-segunda-directiva.directive';

describe('MiSegundaDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new MiSegundaDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
